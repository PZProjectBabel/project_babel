必须加载.github/copilot-instructions.md
回复必须简洁 解释只要关键词
推理必须只要关键词不要可读性
per folder agent.md -> 描述 structure + function, create for code / script folders
coding must update agent.md -> 目标 least token, ignore 可读性
Project Zomboid 多模组AI翻译
语言 .net
目标 github actions linux runner x64
测试 windows x64
严禁修改project_babel_ref内容，允许复制代码，禁止复制数据文件